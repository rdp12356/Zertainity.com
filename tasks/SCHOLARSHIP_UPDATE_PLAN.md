# Scholarship Finder Update Plan

The user provided specific HTML for the Scholarship Finder page. I will update the existing `src/pages/education/Scholarships.tsx` to match this design.

## Proposed Changes

### [MODIFY] [Scholarships.tsx](file:///c:/Users/johan/Documents/Zertainity.com/src/pages/education/Scholarships.tsx)
-   Replace existing JSX with the provided HTML structure.
-   Integrate:
    -   Navigation logic (`useNavigate`).
    -   Material Symbols (using `<span class="material-symbols-outlined">`).
    -   Tailwind classes: Ensure custom colors (`surface-light`, `background-dark`, etc.) are used.

## Verification
-   Manual check of the page at `/scholarships`.
-   Verify responsiveness and dark mode.
